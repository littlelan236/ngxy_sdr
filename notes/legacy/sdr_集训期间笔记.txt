- 干扰：注意自动增益 有可能导致接收失败 可能需要自定义pluto接收块（因为那个块不能实现增益自动控制）
 - 检验接受增益控制：改变Tx板和Rx板的远近看看效果

【实践细节】
-（SRRC滤波器）1，线性时不变滤波器延迟为（抽头数-1）/2个采样点 2，matlab中span参数：num_taps = span * sps + 1 3，在实际条件下（有限冲激响应）滤波器的频率响应是有复数部分的
- BPSK脉冲成型 1，脉冲成型的基本流程是先将符号序列上采样至系统采样率（插0） 2，BPSK的符号是+-1而不是0/1 3，SRRC的冲激响应幅度最大仅约0.4 只过一遍SRRC的话幅度看起来只有0.4 再过一遍就好了
- LMS自适应维纳滤波 先迭代学习一轮 再使用最终的滤波器参数过滤得到输出信号
- 信号功率 1，不能直接dB->计算幅值 这样出来在dB上会差一个固定的数 需要一个修正 如srrc-BPSK这样出来会大8.5dB 2,NMSE的计算时，过滤波前不能autoscale否则NMSE永远小于0 但过滤波后必须autoscale 但要注意NMSE->0-时相当于没有滤波功效（此时不是真的0-）
- 维纳滤波器 1，关于“FFT窗口决定频率响应数量是偶数，这样变换到时域后选中间偶数个截断会不会导致延迟同步有问题” -实际上好像没啥影响 没想象中的敏感 （1）taps129->128 差0.5个采样点NMSE只差0.5dB(2)FFT窗口用偶数 taps=129 这样只会差0.04dB
- FRESH滤波 1，在S/B矩阵中 有些f是超过+-0.5的范围的 利用f的周期性进行归一化
- SCF 1，alpha可能无法正好遍历到循环频率上 但是这个差一点好像就出不来峰值 很蛋疼 【甚至np.arange引发的微小的数值问题都会导致频谱不正确 手敲数字与np.arange出来的图不一样 理论上scf应该是正负对称的 但是实际出出来的负的能量远大于正的 nmd...17位小数都会出问题 但是这个会不会影响不好说 毕竟alphas都是直接指定的 好像没有由于arange引起的数值偏差累积问题 --极小的偏差可能导致shift值差整数 这就导致引起较大偏差】2，pysdr里面的conj_FSM有问题 应该是SCF_slice = np.roll(X, +shift) * np.flip(np.roll(X, +shift +或- 1)) 要不然出来的关于alpha是反的 3，按照FSM改为共轭的逻辑改TSM：将两个slice的偏移改为相同的负值【这里与1，里又不一样了 负值反倒又是对的了】这里-shift-1的-1用不着 应该是针对离散偏移对索引用的 这里用连续频率偏移用不着；将np.conj改为np.flip 4，to_tempral最后到底需不需要fftshift的问题：“scpblog里面是非因果系统 零时刻在中间 所以需要进行fftshift” 虽然这会导致to_spectual与to_tempral不互逆 实践来看好像是正确的
- 共轭SCF S_{xx*}这个东西指的是共轭SCF 与x与x*的非共轭SCF不是一个东西 2，pysdr里面共轭SCF的傅里叶变换后的公式给的是错的
- 卡了半天的问题的最终解决：就是因为pysdr里面给的方法只能应对离散的alpha 就是得按freq_shift来做
- lms自适应维纳滤波：这里的taps和传统意义上的taps不太一样 用的np.dot而不是convolve 所以在得到最终抽头时可能需要flip一下 但是这个也存疑 用正弦信号没法验证到底需不需要flip 出来都差不多 --应该是需要flip的
- NMSE 很奇怪，使用正弦+白噪音的时候 明明出来的波形很好 但NMSE反倒是比滤波前要高 不知道是不是NMSE计算有问题 还是NMSE在这种情况下不能正常反映误差值 --有相位偏差 只自动矫正了幅度 没有自动校正相位（但是对于非正弦信号 相位矫正靠谱吗？）--但是这个相位偏差好像只有在纯正弦波的时候严重一些 2，autoscale也是，只在正弦信号的时候用处大 用rrcbpsk的时候只要能成功收敛 scale基本都是1
- FRESH滤波特性 增加太多的alpha值有的时候反倒不好 反倒会导致滤波效果变差
- 两个LMS 在应对SCPblog中的低SNR场景时 应该延长采样时长 降低学习率 这样是能收敛到不用LMS的结果上的
- RLS 普通的RLS在循环平稳信号上好像效果不佳 至少比LMS出来的要炸一些 --仔细想想是对的，RLS核心是在不断地求维纳解，在噪音强度很大的时候反倒不利
- numpy .T：这玩意对于一维向量没用 因为他的逻辑是对高位数组进行元素位置对调 实现行列向量转换：.reshape(-1, 1)行转列 .reshape(1, -1)列转行
- rls-FRESH RLS实在是太慢了... 129taps就慢的不行 --因为虽然taps是129 但是实际计算的矩阵是 (129*cyc_freqs) * (129*cyc_freqs)的矩阵 太大了 这个算法的时间复杂度是O(n^2) 而RLS是O(n) 太太太慢了... 这玩意算一个64+1taps 32768symbols的都得半天... --不过，原文训练的时间轴单位是samples 只用了~400个采样点就接近收敛了
- 频率偏移集的选择 1，还是加上干扰信号的比较好 BA-FRESH原文不加可能是因为文章的前提是干扰信号循环频率未知 2，加过多的循环频率反倒会导致无法收敛 sparce3A还是很有含金量的
- BA-FRESH 1，出于以上原因，感觉得加上干扰信号的循环频率 2，对于比赛的情景 应该只把按alpha频移的信号作为参考信号 beta那边干扰和信号的能量是重叠的 但是对于CSPblog那边的情景 又不太好说
感觉BA-FRESH不行就弃用。如果可以用随机序列先训练一个差不多的taps 就不管这个BA了
这个滤波器可能是因为使用情景不同导致的没法用 如果要验证可能要从实数信号FRESH弄起


短期
尝试无噪音解码
尝试0dB解码
尝试循环频率解码


疑问
- 预训练参数在不同结构/参数量的网络中能否互通？
- 采样对齐/
- 数据升维就是好多个不同参数相同形状的卷积合在一起？
- 什么是前处理后处理
--》如何通过升维/前后处理来有效利用上“循环频率”这个信息？
- "RNN/LSTM模型解决了序列数据处理的问题" 序列到序列 编码器-解码器
- 模拟信号的生成问题 这个正交鉴频真的会使结果是简单的两个波形的线性叠加吗？很怀疑这个问题 也许可以实际上手尝试验证一下这个 
- 有关文献中的网络 什么叫同步块OE也能一起训练？这个玩意是没有梯度信息的

有用的想法
- 二段式学习 先使用模拟的信号 再使用真实的信号 -》真实信号如何解决同步的问题？？--一个发送buffer中先全是0 再仅发送一轮有用信息即可  但是这个对于需要两路输出的干扰+信息好像不行  --可以使用直线+正弦波，在一个gnuradio里面使用两路输出 看看两个设备的开启延迟 如果有延迟的话也许可以使用gnuradio里面的控制系统控制一下？
- 加噪模型 这里是有采样时间误差 频移误差和相位误差的
- 数据集鲁棒性 上述误差 还有一个噪声信号强度 （甚至可以用来搞二级干扰？）还有一个中等分贝的噪声（对面那个发射机也可能有影响） 整体强度线性缩放过的信号
- 原始数据和循环频率变换的信号都输入进去？// 原始信号和经过FRESH滤波的信号都输进去？--尝试在高SNR下训练的滤波器能否适配低SNR 尝试仅循环频率不同的信号FRESH的效果如何
- 对于FSK 对CFO和phaseoffset的处理方式应该是不同的 这两步应该还是影响不大
- 感觉这个使用情景不适合dropout


【长期
 - rrc滤波器的问题 gnuradio的源码里面找不到有关rrc滤波器具体计算的代码
 - （这个最后再说）问一问算法 串口和裁判系统对接的时候有没有误码的问题 是怎么处理的 这个涉及到帧同步
 - 联系与裁判系统对接
